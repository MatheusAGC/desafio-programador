services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: quickfiller
      POSTGRES_PASSWORD: quickfiller123
      POSTGRES_DB: quickfiller
    ports: 
      - "5432:5432" 
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: